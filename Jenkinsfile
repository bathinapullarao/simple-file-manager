def server_id=Server
pipeline {
          stages {
		         stage ('checkout') {
			                     steps {
				                    checkout scm
				                   }
		                             }
                        }
           }
